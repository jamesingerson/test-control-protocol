# Outstanding items

Backlog from the grammar/colour review and testing work-through. Grouped by area; `[x]` = already done in this pass, `[ ]` = still open.

## Grammar correctness

- [x] **Title-line scope mislabelled as `hex-line`.** `title-lines` was created by copy-pasting `hex-lines` (confirmed via `git show 53a899c`) and its overall scope name was never updated. Fixed in `syntaxes/testcontrolprotocol.tmLanguage.json` — now `title-line.testcontrolprotocol`. Caught by the new snapshot tests before it could get baked in as "expected" behaviour.
- [x] **Highlighting silently disappearing for some users, depending on colour theme.** `support.type`, `variable.parameter`, and `entity.name.function` have no colour rule at all in VS Code's bundled "Visual Studio" legacy themes (`dark_vs.json`/`light_vs.json`), only in the modern Dark+/Light+ defaults — confirmed by diffing the grammar's scopes against VS Code's own theme source. Remapped to scopes with real coverage in both: `support.type` → `storage.type`, `variable.parameter` → `entity.other.attribute-name`, `entity.name.function` → `entity.name.tag`.
- [x] ~~Trailing literal `\n` in `invalid`/`comments` match patterns could fail to match the last line of a file lacking a trailing newline~~ — investigated empirically via `vscode-tmgrammar-snap` (the real VS Code tokenizer) across single-line files, multi-line files, with and without a trailing EOF newline. All matched correctly in every case tested. Not an actual bug — no action needed.
- [x] **`goto-lines` keyword alternation was inconsistent.** Fixed: wrapped the alternation in `(?:...)` so `\b`/`\s` apply uniformly to all four keywords, and capped `SEARCH` at the same 8-char field width as the others. Verified via the snapshot tests — the keyword field now spans its full 9-column width instead of leaking a stray space into the next field.
- [x] **Folding markers didn't match the grammar's own column convention.** Fixed: `language-configuration.json` now uses the fixed `.{7}` line-number field and includes `Q` as a valid title-line type in the fold start marker. Not covered by the snapshot tests (they only exercise tokenization, not folding) — worth a manual F5 check.
- [x] **`get-lines` duplicated `goto-lines`.** Its capture-scope mapping was byte-identical to `goto-lines`'s, so `GET` is now folded into the same keyword alternation instead of being a separate copy-pasted block. `gotcp-lines` and the generic `instruction-lines` fallback stay separate by design (each assigns a genuinely different scope to at least one field) — considered restructuring them into nested begin/end sub-patterns to share more code, decided against it: no visible benefit, and it would introduce a structural pattern (begin/end) not used anywhere else in this grammar for the sake of DRY alone.

## Testing

- [x] Added snapshot-based grammar tests (`vscode-tmgrammar-snap`) — `npm test` / `npm run test:update`, fixtures under `tests/fixtures/*.tcp`. One fixture is transcribed from real content (`images/tcp-highlighting-example.png`); the rest (`H`, `N`/`R`/`S` lines, `GOSUB`/`SEARCH`/`DATE`/`GOTCP`/`GET` operand shapes, `D`/`G`/title-line widths) are re-created generically rather than copied verbatim, but their structure has since been validated against both the manuals and real production TCP files — see the manual/production cross-reference sections below for what's been checked and how.
- [x] Added `.gitignore` (was missing entirely — `npm install` would otherwise have staged `node_modules`).

## Manifest / packaging polish

- [x] **`"extensions": [""]` was dead weight.** Removed — the README already requires every user to manually set `files.associations`, so this empty-string extension entry never matched anything real.
- [x] **No `CHANGELOG.md`** despite repeated version bumps — added, with a 0.1.0 entry covering this pass; versions prior to 0.1.0 aren't backfilled (see `git log`).
- [x] `author` attribution decided: stays personal (`"James Ingerson"`) rather than switching to a Pathlab org — no code change needed, it already read this way.

## Version bump

- [x] Bumped to **0.1.0** (minor, per instruction — not the major bump floated earlier) to cover this pass of fixes.

## Manual cross-reference findings (2026-07-30)

The user provided the real Delphic LIS "TCP Introduction Manual" and "TCP Reference Manual" (see memory: authoritative spec, not to be reproduced verbatim). First time the grammar's been checked against real documentation instead of reverse-engineered example files. See `git log` for what's implemented from this list; items get checked off as they land.

- [x] Confirmed **correct, not a bug**: `gotcp-lines` coloring the GOTCP target as `storage.type` rather than `entity.name.tag`. The manual confirms GOTCP's operand is a 4-digit numeric test code, not a label — resolves the open question left in the duplication item above.
- [x] **SEARCH's item operand is a fixed 12-word vocabulary, not free text** (`PATIENT NAME REQUEST DATETIME ARRSET REPORTED TRACKED TEST GROUP CONSTIT ANTIBODY PRODUCT TESTDEPT`) and was colored as generic `string`. Split `SEARCH` out of the merged `goto-lines` rule into its own `search-lines` rule (its operand shape differs from GOTO/GOSUB/DATE/GET's — a real, not cosmetic, un-merge) and validated the item word against the documented list, scoped `constant.language`. Unrecognised words fall through to the generic `instruction-lines` fallback rather than being flagged `invalid` directly — see commit for why.
- [x] **Condition-code suffixes on GOTO/GOSUB were unvalidated** — any 1-4 char suffix was accepted as `keyword.control`. Tightened to the documented fixed list (`EQ NE GT GE LT LE IR OR M MM`); `DATE` requires one of the numeric-comparison subset (`EQ NE GT GE LT LE`), no bare form; `GET` tightened to its own documented suffix set (`,E` `,O` `,A`, or bare); `GOTCP` (found via real-data scan, not the manuals) also takes the same six comparison codes, tightened to match. A line with an unrecognised suffix no longer matches these rules at all and falls through to the generic `instruction-lines` fallback — visually, the target field loses its `entity.name.tag` colour instead of turning red; documented in the relevant commit rather than treated as a "flash red" validation.

### Cross-checked against real production data (2026-07-30)

The user confirmed `TCP/MACRO` and `TCP/GLOBAL` are both genuinely opened with this extension, and pointed at the real production TCP repo (`C:\repos\TCP`, read-only — not to be modified). Precisely column-anchored scans (not loose text search, which produced false positives at first — e.g. an unanchored check made bare `DATE` look common when it was actually always appearing as an operand *value* like `PRINT 21 DATE`, never as the instruction keyword) across ~378 files / 100k+ lines:

- [x] Every GOTO/GOSUB/DATE/GET/GOTCP condition-code suffix actually used in production matches the tightened, documented set exactly — zero unrecognised suffixes found. Confirms the item-5 tightening above doesn't reject anything real.
- [x] SEARCH's item-operand word is always one of the documented 12, and SEARCH never takes a condition-code suffix (2638 real occurrences, all bare `SEARCH`) — confirms the search-lines split above.
- [x] **`D` lines fixed.** Real `D`-lines in `MACRO` confirmed the manual's shape (macro name, not code+name+units) but revealed an extra undocumented detail: an optional free-text note can follow the name (e.g. `D BLANKMC1 NEWLINE`), with no fixed column boundary for where it starts — some names fill the full 8-char field with no gap at all before the note. `title-lines` no longer matches `D` (now just `[TQ]`); added a dedicated `macro-definition-lines` rule using `[^ ]{1,8}` for the name (stops at the first space naturally, unlike `.{1,8}\s` used elsewhere, which would either fail to match names with no trailing content at all or risk swallowing into the note on short names — verified this matters using a real short-name+trailing-content shape). Verified against the entire real `MACRO` file (13,638 lines) — zero unstyled spans.
- [x] **`G` lines fixed.** The real `GLOBAL` file's header line is literally the text `GLOBAL ALPHA DATA` starting at column 8 (the `G` of "GLOBAL" *is* the type-code character) — confirmed exactly as the manual described. Added a `global-header-lines` rule matching that literal, scoped `markup.heading`. Verified against the entire real `GLOBAL` file — zero unstyled spans.
- [x] **`M` line format: decided to accept the current `A`/`M` merge as-is, unresolved by design rather than by evidence.** Searched the entire real production repo (~378 files) for a column-8 `M` line and found zero (the single early `M` hit was from `.gitignore`, not a TCP file); the manual only implies Mask lines exist via error messages (Reference Manual Assembly Error 35, p.314; System Errors 15/16/24, p.317) without ever specifying their format. No real data exists either way to confirm or refute the merge. User's call: Mask lines are likely a meta/utility feature for the third-party vendor's own development use, not something authored by end users at Pathlab — so the current merge is accepted permanently rather than left as a pending question. Do not re-raise this in future reviews unless a real `M`-line example actually surfaces.
- [x] All four real files spot-checked (`GLOBAL`, `MACRO`, `HAEM`, `BIO`, one `SEARCH/` file — ~38,000 lines combined) tokenize with **zero unstyled spans** after the D/G fixes (excluding the line-number field, which is deliberately uncoloured by design).
- [x] ~~Trailing comments past column 55 flagged `invalid` (red) — confirmed intentional, not a bug.~~ **Superseded 2026-07-30 — see "Trailing/overhanging comments enabled" section below.** The manual documents column-55+ free text as a legal inline comment, and real `HAEM`/`BIO` lines use it (e.g. `I COPY     COPYTO   51456                          copy to Hosp PHAR`). Raised this as a cross-cutting finding since every rule's final catch-all group scopes that region `invalid.testcontrolprotocol`. At the time, the user confirmed this was a deliberate choice, not an oversight: the real-world implementation of trailing comments is poor and creates ambiguity and compilation errors, so flagging them as invalid was intentionally discouraging their use even though the format nominally permits them. That decision has since been revisited after further review of production files — this entry is kept for history, not as current guidance.
- [x] **Title-line field widths were not just imprecise — they were actively too narrow, wrongly flagging real content invalid.** The old regex budgeted 18+19=37 characters total for name+units combined (from col13); the manual's real column layout (name cols 14-29, units cols 31-50) needs 17+20=37... but that's only correct when both fields are used. Real data showed a third, undocumented case: **when a title has a long descriptive name and no units at all, authors just let the name run past col29 with no separator** — confirmed directly (`T0013 ES300 General Link    - Look at TCP/LINK`, `T0527  HORMONE TESTS...   Pathlab WCP`, etc.). Quantified impact: **51 of 3977 real `T`/`Q` lines (~1.3%) had legitimate trailing text (`- Look`, `Ref Range`, `Pathlab WCP`, `Porphyrins`, ...) wrongly rendered `invalid` (red) today**, not a theoretical precision gap. Fixed by widening the two fields (17 + 62 chars) so nothing legitimate on a well-formed ≤80-char line can fall past them into the overflow group — verified against all the real overflow examples found, all now render with zero `invalid` spans. Also tightened the code field from `.{4}` to `\d{4}` since all 3977 real codes are purely numeric, with zero exceptions.
- [x] **GOTO/label verification feature** — built. See "Label and GOTCP verification feature (2026-07-30)" below for the full design, the two critical corrections that came out of validating against real production data, and how it was tested.

## Label colour unification (2026-07-30)

- [x] **`entity.name.tag` was used for both a label declaration (an I-line's own label) and a reference to a label (a GOTO/GOSUB/DATE/GET/SEARCH target) — same colour.** Initially misread as a bug needing a split (see the false-start below); the user clarified the actual goal is the opposite — declarations and references should intentionally share one colour (they're both "a label"), just not the *original* colour, since that also collided with other unrelated scopes. Final state: both now use `entity.name.function`, confirmed via `scripts/resolve-theme-colors.js` to render as a colour (`#DCDCAA` Dark+, `#795E26` Light+) genuinely distinct from the marker (`entity.other.attribute-name`), `keyword.control`, `string`, `comment`, and `invalid` — the things it actually needs to be distinguished from.
- **False start, corrected**: first tried splitting declaration/reference into two scopes (`entity.name.tag` + `variable.language`, then `entity.name.tag` + `entity.name.label`) before the user clarified the real goal was unification, not separation. Along the way this surfaced a real, separate methodology bug: `variable.language` was verified as having a colour *rule* in `dark_vs.json`, but never checked against whether that rule's actual hex value differed from `entity.name.tag`'s — it didn't (both `#569cd6`), and since `dark_plus.json` only *includes* `dark_vs.json` without overriding either scope, the collision carried through to Dark+ too. **Lesson, now encoded in `scripts/resolve-theme-colors.js` rather than just written down**: "has a colour rule" and "renders as a visually distinct colour from another scope in use" are different checks.
- [x] **Added `scripts/resolve-theme-colors.js`** (+ `scripts/download-themes.js` to fetch the theme JSON it reads) — resolves the actual effective colour VS Code would render for every scope this grammar uses, against a given theme, following `"include"` chains and real TextMate specificity rules, and flags any of our own scopes that collide. Run `node scripts/download-themes.js` once, then `node scripts/resolve-theme-colors.js [theme files...]`. This is what caught the `variable.language`/`entity.name.tag` collision precisely, after two LLM-summarized web-fetches of the same file gave contradictory answers about the exact same string ("does dark_plus.json override `storage.type`?") — parsing the raw JSON directly resolved the ambiguity for good. Use this tool for any future colour-collision question on this grammar instead of guessing from memory of past fetches.
- [x] **Two more instances of the same "coverage fix regressed the Dark+ colour" mistake, caught by the user reviewing real content.** Both `storage.type` (title-line code/name, GOTCP target) and `constant.language` (SEARCH's item keyword) were teal/orange respectively before an earlier session pass moved them to fix legacy `dark_vs`/`light_vs` coverage gaps — that fix cost the Dark+ colour without checking what it actually became. Confirmed via `scripts/check-one-off.js` (ad-hoc, not committed) that `support.type` restores the teal (`#4EC9B0` Dark+, `#267f99` Light+, unstyled on legacy `dark_vs`/`light_vs` — same accepted tradeoff as `entity.name.function`) and plain `string` restores the orange for SEARCH's item keyword. Reverted `storage.type` → `support.type` (title-lines, gotcp-lines) and `constant.language` → `string` (search-lines). This also shrank the previously-logged 4-way Dark+ collision down to just `markup.heading`/`storage` (`#569cd6`) — the `G`-line header and number/alpha-line's own label — a much smaller, lower-stakes residual gap, still open below.
- [x] ~~Known remaining collision: under Dark+, `markup.heading` (the rare `G`-line header) and `storage` (number/alpha-line's own label field) both resolve to `#569cd6`.~~ **Resolved as a side effect (2026-07-30)**: the `G`-line header was recolored to `support.type` (teal, on the user's request, matching the `D`/`T` declaration colour), so `markup.heading` isn't used anywhere in the grammar anymore — nothing left to collide.

## Number-line (`N`/`R`/`S`) field boundary bug (2026-07-30)

- [x] **`invalid` was starting at different columns depending on whether the trailing annotation text happened to look numeric.** The old regex matched each of the up to 4 numeric fields by *content* (`[+-.]?\d*[+-.]?\d*\s+`), and every component of that pattern is individually optional — meaning it would happily match a phantom "field" out of nothing more than "starts with a digit/sign/dot, then some whitespace." Real content confirmed this precisely: a trailing annotation like `1-3   "` or `70  Years` (both start with something digit-like) had its leading run swallowed as a fake 4th data field, leaving only the non-numeric tail (`"` or `Years`) marked `invalid`; an annotation like `< 1 month` or `adult` (starts with a non-digit) correctly fell into `invalid` in full. Same start-line, different end result, purely by coincidence of the first character.
- User's call: "make it naïve and consistent" — stop trying to recognise numbers by content, use pure fixed-width positional fields instead (the `.{1,8}\s` idiom already used for every other field in this grammar), so the boundary is the same real column regardless of content. Two iterations to get right:
  1. First pass made only 3 of the 4 fields require a trailing separator, leaving the last one width-capped but not separator-anchored — this let *short* trailing text (`adult`, 5 chars) fit entirely inside the last field's own slot and render as plain `string` with no `invalid` at all, while longer text (`70  Years`, 9 chars) only partially overflowed into `invalid`. Caught this myself before committing and flagged it as a real trade-off.
  2. The user's real screenshot showed neither the original bug's behaviour *nor* that first-pass trade-off — everything rendered fully `invalid` regardless of length, meaning the real file's field spacing is wider than my synthetic test fixture assumed. Made all 4 fields require the same trailing separator (`.{1,8}\s` × 4, matching the standard 9-char field width used everywhere else in this grammar), and rebuilt the fixture with realistic (fully 9-char-padded) spacing to match. Confirmed against the corrected fixture: both `70  Years` and `adult` now render fully `invalid`, matching the real screenshot exactly.
- **Lesson**: a synthetic fixture's exact spacing matters as much as its content when testing a positional/column-based grammar — a fixture padded too tightly can pass tests while silently not exercising the real-world column layout, producing a false sense of verification. Worth erring toward generous, standard-grid spacing (9-char fields, matching `editor.rulers`) in fixtures rather than the tightest spacing that happens to fit the sample values.

## Trailing/overhanging comments enabled (2026-07-30)

- [x] **Reversed the earlier "trailing comments are intentionally invalid" decision, on the user's explicit instruction after reviewing more production files.** The catch-all "content left over after a line's declared fields" capture in `hex-lines`, `number-lines`, `goto-lines`, `search-lines`, `gotcp-lines`, `instruction-lines`, and `title-lines` is now scoped `comment.line.testcontrolprotocol` instead of `invalid.testcontrolprotocol`. Deliberately excluded `alpha-lines` (stays `invalid`) — the manual says the trailing-comment convention doesn't apply to `A` lines, and this was checked against real production data before finalizing: the `A`-line value field already spans columns 19-79 (61 chars), leaving only column 80 as possible overflow before `invalid.too-long` takes over at 81 — structurally too narrow for a separate comment to exist. 15 real `A`-lines hit exactly 80 characters and none show a "value + gap + comment" shape; they're just long legitimate data strings using the full field width.
- **No new regex needed for the 80-column boundary.** The top-level `invalid.too-long` rule (`^.{81}.*`, 81+ characters) is checked before any line-type rule runs, so it already intercepts anything past column 80 before the line-type rule's own (now-comment) catch-all is ever reached. `tests/fixtures/trailing-comments.tcp` confirms this precisely at the actual boundary (not just "comfortably under vs. comfortably over," which wouldn't catch an off-by-one): a 60-char line's trailing text renders as `comment.line`; the same shape at **exactly 80 characters** still renders as `comment.line` in full; at **exactly 81 characters** the *entire* line flips to `invalid.too-long`. (An earlier version of this fixture used an arbitrary 89-char line instead of the precise 81-char edge — technically covered "well past the limit" but wouldn't have caught a one-character-off boundary error. Fixed after being asked what the 89 was actually verifying.)
- Planned via `EnterPlanMode`/`ExitPlanMode` given the scope (touches most of the rule set and reverses a previously-settled decision) — see the approved plan for the full design reasoning if useful context resurfaces later.

## Number-line label field swallowed its own comment (2026-07-30)

- [x] **`number-lines`' label field (`group3`) used `.{8}\s*` — exactly 8 characters, then *unbounded* greedy trailing whitespace — unlike the `.{1,8}\s` idiom used by every other field in this grammar, including fields 4-7 in this same rule.** With no real numeric values present (e.g. a bare `R VRANGE` label with an empty range, followed by a trailing comment), that greedy `\s*` swallowed the entire blank gap in one go, landing the still-optional numeric field slots exactly on top of the comment text — which they then consumed as if it were data (e.g. `Venous range` rendered entirely as `string`/orange, not `comment.line`/green). Fixed by bounding it to `.{1,8}\s`, matching the fields that follow. Confirmed against the user's exact reported case plus every other line from the same screenshot (`RRANGE`, `IRANGE`, `STRANGE`, `URANGE`, `RWARN`) — all render correctly now. Added a dedicated regression fixture (`R VRANGE` with all four fields blank).
- [x] ~~Found the same bug class in `search-lines`, unconfirmed against real data.~~ **Attempted and reverted (2026-07-30) — decided not worth fixing.** The item-keyword field (`group6`) ends in unbounded `\b(?:...)\b\s*`, same shape as the number-lines bug. Tried bounding it to a single mandatory `\s` (matching the number-lines fix) — this fixed the synthetic blank-criteria-plus-comment case, but broke a real, already-established one: short criteria values like `FIRST` (matching confirmed real values such as `TV2`/`20`) got their tail pushed into the comment scope, because the item keywords have varying lengths (`TEST`=4 chars, `DATETIME`=8) and a single fixed bound can't account for the different leftover padding each one leaves. The only fully correct fix would give each of the 12 keywords its own hand-computed padding width — verbose and fragile for a trigger combination that's never been observed in any real production file (263 real lines have a blank criteria field, zero of those also have a trailing comment). User's call: revert, not worth it. `group6`/`group7` are back to their original `\s*`/no-separator form.

## Macro-name colour (2026-07-30)

- [x] **Macro declarations (`D` line names, e.g. `DTMCR`) recolored from `entity.name.function` (yellow) to `support.type` (teal), on the user's request, to match `T`-line title declarations rather than looking like a label.** Semantically reasonable too — a macro definition is closer to "declaring a named thing" like a title than to a branch-target label. Only affects `macro-definition-lines`' own capture; unrelated to the label-declaration/reference colour (`entity.name.function`, still yellow) discussed earlier this session.
- [x] **Follow-up: the `D` marker itself (the leading type-letter, previously `entity.other.attribute-name` like every other line type's marker) is now also `support.type`, so `D` + the macro name render as one consistent teal unit** — matching how `T`-lines already treat their type-letter+code as a single `support.type` capture, rather than the marker looking separately colored from the name it introduces.

## Label and GOTCP verification feature (2026-07-30)

Built on explicit instruction, with scoping confirmed up front via three quick questions (trigger timing, GOTCP validation scope, `~`-label handling), then developed autonomously against real production data. First real extension-activation code in this repo (`main`/`activationEvents` in `package.json`, `vscode.languages.createDiagnosticCollection`) — everything before this was a declarative grammar only.

**Architecture** (plain JS, no build step, matching the rest of this repo):
- `src/labelParser.js` — pure regex-based extraction (label defs, GOTO/GOSUB/DATE/GET/SEARCH-op1 references, GOTCP targets, macro-invocation opcode fields, T/Q-block boundaries, D-macro-block boundaries). Regexes are hand-mirrored from the exact `.tmLanguage.json` match patterns (`goto-lines`/`search-lines`/`gotcp-lines`) so highlighting and diagnostics never disagree about what counts as a keyword/target/field. No `vscode` dependency — unit-tested with plain Node (`tests/label-parser.test.js`).
- `src/diagnostics.js` — pure orchestration: given a document's lines (+ optional macro/GOTCP workspace index), returns plain diagnostic descriptors. Unit-tested (`tests/diagnostics.test.js`).
- `src/workspaceIndex.js` — pure aggregation of macro-name→labels and test-code sets across a list of already-read `{uri, lines}` documents. Unit-tested (`tests/workspace-index.test.js`).
- `src/extension.js` — the only `vscode`-dependent file. Debounced on-change diagnostics per open document (~500ms), debounced workspace-index rebuild on file save/create/change/delete (~1000ms). Not unit-testable without a real Extension Host; verified with `node --check` (syntax only) and careful manual review against the real `vscode` API — **not exercised live in an actual Extension Development Host**, since this session has no GUI access to VS Code. Worth a manual F5 sanity check.

**Two corrections that came from validating against the real production repo (`C:\repos\TCP`, read-only) before trusting the design, both load-bearing — skipping either would have made the feature actively wrong, not just imprecise:**

1. **Labels are scoped per T/Q test-definition block, not per physical file.** A single file can bundle hundreds of scripts — `BIO` alone has 623 separate `T`/`Q` headers in one physical file. Naively treating "duplicate label" as "same label name anywhere in this file" produced 2063 false positives in that one file alone (e.g. `FIN` legitimately reused as a per-test terminal label 532 times). Scoping every check (duplicate labels, undefined labels) to the span between one `T`/`Q` header and the next dropped that to zero across the entire real corpus.
2. **Macro invocations inject labels into the calling block at assembly time.** A bare instruction line like `I DRUGM DRUGMCR` isn't a GOTO/SEARCH/GOTCP — `DRUGMCR` is a macro name (defined elsewhere, e.g. the real `MACRO` file, via `D DRUGMCR`), and its body's own labels (`CONT`, `DCHECK`, `ENDMCR`, ...) become valid targets inside any block that invokes it, even though they never appear in that block's own text. Confirmed via a real case: `BIO`'s `T0750` block references label `CONT`, which only exists inside the `DRUGMCR` macro it calls. Fixed by indexing every macro's internal labels workspace-wide and unioning them into a block's known-label set whenever that block's opcode fields match a known macro name. Related: macro *bodies themselves* are template code using an `OP1`/`OP2`/`OP3`/... parameter-placeholder convention (substituted by the caller's real arguments at assembly time) — confirmed in the real `MACRO` file (e.g. `D DTCP,EQ` contains `GOTCP,EQ OP1 ...`). Diagnostics deliberately never run *inside* macro bodies for this reason; only inside `T`/`Q` blocks.

**Validation**: after both corrections, ran the actual `diagnostics.js`+`workspaceIndex.js` end-to-end against the entire real production corpus (26 files, 3815 test blocks, 253 macros) — **zero diagnostics produced**, i.e. zero false positives against known-good, currently-running production scripts. This is the strongest evidence available that the feature's core logic is sound, short of live use.

**Live false positive found and fixed (2026-07-30, after initial build):** a real GOTCP reference in production file `CONTROL` (`GOTCP 4998`, targeting `T4998` which is defined in a completely different file, `HAEM`) was flagged as not found. Root cause: `getWorkspaceGlobPatterns()`/`collectWorkspaceUris()` fed `files.associations` pattern strings (e.g. `"**/TCP/**/*"`) directly into `vscode.workspace.findFiles()`. That API matches its glob against each file's path **relative to the workspace root**, while `files.associations` matches against a file's **full path** — different semantics. When the workspace root is opened as `C:\repos\TCP` itself (as it is for the real production repo), no relative path ever contains a `TCP` segment, so `findFiles('**/TCP/**/*')` silently matched zero files, and the workspace index quietly degraded to "whatever's currently open" — `HAEM` wasn't open, so `T4998` was invisible. Fixed by adding `src/globMatch.js` (pure, unit-tested glob matcher) and scanning broadly with `findFiles('**/*')`, then matching each result's full path against the association patterns ourselves, exactly mirroring how `files.associations` actually resolves. See `tests/glob-match.test.js`, which reproduces the exact real pattern/paths from this bug report.

**Known limitations, not fixed here:**
- No macro-expansion beyond label-name injection — a macro's internal `GOTO`/`GOTCP` references (which use `OP1`/`OP2`/`OP3` placeholders) are never themselves checked, by design (see above).
- `~`-prefixed macro-internal labels (disambiguated to `~1`/`~2`/... only at assembly time) are exempted from every check, not partially validated.
- The regex duplication between the grammar and `labelParser.js` (noted in that file's header comment) has no automated check tying them together — if one changes, the other must be updated by hand.
- The workspace-wide file index relies on `files.associations` (since real production TCP files are extensionless — `BIO`, `MACRO`, `GLOBAL` have no `.tcp` suffix); if a user hasn't configured any association for this language, workspace-wide GOTCP/macro resolution silently degrades to only the currently-open documents. Diagnostics for a document's own internal labels are unaffected either way.
